<!DOCTYPE html>
<html lang="en">
<head>
  <style type="text/css">
    body{
      padding: 0px;
    }
    .center {
    margin-left: auto;
    margin-right: auto;
    }
    .center.navbar .nav,
    .center.navbar .nav > li {
    float:none;
    display:inline-block;
    *display:inline; /* ie7 fix */
    *zoom:1; /* hasLayout ie7 trigger */
    vertical-align: top;
    }
    .center.navbar-inner {
    text-align:center;
    }
    .center .dropdown-menu {
    text-align: left;
    }
    .navbar-inner {
    margin: 0 auto;
    }
  </style>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "web_app" %></title>
  <%= csrf_meta_tags %>

  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
      <![endif]-->

      <%= stylesheet_link_tag "slate", :media => "all" %>

      <%= yield(:page_stylesheet) if content_for?(:page_stylesheet) %>
    </head>
    <body>
      <!-- Sitewide Navbar -->
      <nav class='navbar navbar-default navbar-static-top' role='navigation'>
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="mypage">Meeting Ninja &#8482;</a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav navbar-right">
            <p class="navbar-text">Welcome <a href="#" class="navbar-link">Tony Stark</a>!</p>
            <li><a href="#">Meetings <span class='glyphicon glyphicon-time'></span></a></li>
            <li><a href="#">Tasks <span class='glyphicon glyphicon-tasks'></span></a></li>
            <li><a href="#">Groups <span class='glyphicon glyphicon-globe'></span></a></li>
            <li><a href="#">Settings <span class='glyphicon glyphicon-cog'></span></a></li>
          </ul>
        </div><!-- /.navbar-collapse -->
      </nav>
      <%= yield %>

      <footer>
        <nav class='navbar navbar-fixed-bottom navbar-centered navbar-inner center'>
          <div class='collapse navbar-collapse' id='bs-example-navbar-collapse-1'>
            <ul class='nav navbar-nav'>
              <p class='navbar-text'><a href="#" class="navbar-link">About</a></p>
              <p class='navbar-text'><a href="#" class="navbar-link">Contact Us</a></p>
              <p class='navbar-text'><a href="#" class="navbar-link">Privacy Policy</a></p>
              <p class='navbar-text'><a href="#" class="navbar-link">Terms of Use</a></p>
              <p class='navbar-text'><a href="#" class="navbar-link">Help</a></p>
            </ul>
          </div>
        </nav>
      </footer>
    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "slate" %>
    <%= yield(:page_javascript) if content_for?(:page_javascript) %>
  </body>
  </html>
